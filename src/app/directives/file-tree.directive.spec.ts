import { FileTreeDirective } from './file-tree.directive';

describe('FileTreeDirective', () => {
  it('should create an instance', () => {
    const directive = new FileTreeDirective();
    expect(directive).toBeTruthy();
  });
});
