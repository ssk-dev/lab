import { ConvertStringToCasePipe } from './convert-string-to-case.pipe';

describe('ConvertStringToCasePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertStringToCasePipe();
    expect(pipe).toBeTruthy();
  });
});
