import {ConvertToLinkPipe} from './convert-to-link.pipe';

describe('ConvertToLinkPipe', () => {
  it('create an instance', () => {
    const pipe: ConvertToLinkPipe = new ConvertToLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
