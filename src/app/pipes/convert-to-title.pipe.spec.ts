import { ConvertToTitlePipe } from './convert-to-title.pipe';

describe('ConvertToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
